# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- "*"

stages:
  - stage: test
    jobs:
      - job: 'validateandtest'
        pool:
          vmimage: windows-latest
        steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'SPN_OSODLDev'
    subscriptionId: 'aba57ba1-7981-431a-b3e5-dd9f0c34f14c'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'TOS-ENP-LAB-RGP-002'
    location: 'Australia East'
    templateLocation: 'URL of the file'
    csmFileLink: '$(Build.SourcesDirectory)/StorageAccount/azuredeploy.json'
    csmParametersFileLink: '$(Build.SourcesDirectory)/StorageAccount/azuredeploy.parameters.json'
    deploymentMode: 'Validation'